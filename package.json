{
  "name": "kennie-soberboy-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static Kennie Soberboy website prepared for Vercel deployment.",
  "scripts": {
    "verify:assets": "node scripts/verify-assets.js",
    "build": "node scripts/build-static.js && npm run verify:assets",
    "preview": "npx http-server dist -p 8080"
  },
  "engines": {
    "node": ">=18"
  }
}
